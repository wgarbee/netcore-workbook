@using ToDoApp.Services
@model ToDoApp.Models.Status
@inject IRepository repository
@{
    var items = repository.Statuses.Select(x => new SelectListItem(x.Value, x.Id.ToString()));
}

<select asp-for="Id" asp-items="@items"></select>